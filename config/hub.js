// ============================================================================
// 📁 config/hub.js —— 中枢配置【用户领地：框架更新永不触碰此文件】
// ============================================================================
// 只放"跨域/混册"层面的东西。各域自己的配置在 config/card.js、config/checkin.js。

export const HUB_CONFIG = {
  mixedCalName: '📌 提醒合集',   // ?cal=all 或多域组合时的册名
  mixedColor: '#FF9500',        // 混册整册颜色(?color= 可临时覆盖；带 # 需编码 %23)
  timezone: 'Asia/Shanghai',

  // ── 中枢窗口(v6)：?past= / ?future= 可临时覆盖 ────────────────────────────
  // 【窗口是许可边界,不是生产配额】域只被约束"不得越界",从不被要求"填满":
  //   · 签到域算法天生只从锚点向前推 → past 区间自然空产出,框架不催产;
  //   · 信用卡域回溯有真实价值(核对推算、暴露 bug) → 它用足这个许可。
  // 单位为月;框架换算成绝对日期区间下发给各域(月对不同域语义不同,绝对日期才无歧义)。
  // v6 起这是【唯一】的跨度参数 —— 旧的 ?months=(签到域)已废除,写了会在诊断里响亮告警。
  pastMonths: 3,
  futureMonths: 12
};
